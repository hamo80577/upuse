# Server
PORT=8080
# Existing encrypted settings were created with the persisted data key.
# Rotate this secret only after re-encrypting the stored tokens.
UPUSE_SECRET=dev-secret
# UPUSE_SECRET_PREVIOUS=
